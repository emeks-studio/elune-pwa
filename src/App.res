%%raw("import './App.css'")

@react.component
let make = () => {
    <div className="App">
        <header className="App-header">
        <p>{React.string("Elune PWA")}</p>
      </header>
    </div>
};
